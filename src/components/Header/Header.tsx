import React from 'react';

const Header = () => {
    return (
        <header style={{outline:'1px solid red'}} className={'header'}>
            Header
        </header>
    );
};

export default Header;