import React from 'react';

const Footer = () => {
    return (
        <div style={{outline: '1px solid red'}} className={'footer'}>
            Footer
        </div>
    );
};

export default Footer;