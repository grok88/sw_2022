import React from 'react';
// @ts-ignore
import styles from './Footer.module.css';

const Footer = () => {
    return (
        <div style={{outline: '1px solid red'}} className={styles.footer}>
            Footer
        </div>
    );
};

export default Footer;